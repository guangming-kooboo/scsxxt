@model ServicePlatform.Areas.App.ViewModel.Home.PracticeCaseList_M

<!DOCTYPE html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    @*<meta name="viewport" content="width=device-width, initial-scale=1">*@
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1, maximum-scale=1, user-scalable=no" />
    <title>案例列表</title>
    <link href="~/Areas/App/Content/CSS/css.css" rel="stylesheet" />

</head>
<body>
<div class="main-do">
    @*<div class="note-head2">
        <ul class="note-ul">
            <li class="note-li-main"> <a href="@Url.Action("Main", "Home", new {area = "App", uid = Model.Uid})">首页</a></li>
            <li class="li-one"><a href="@Url.Action("Main", "Home", new {area = "App", uid = Model.Uid})"><</a></li>
            <a class="note-a" href="@Url.Action("DiaryList", "Home", new {area = "App", uid = Model.Uid})"> <li class="note-li">周记(@Model.DiaryCount)</li></a>
            <li class="li-three"><p></p></li>
            <li class="note-lis">案例(@Model.CaseCount)</li>
            <li class="li-two"><p></p></li>
        </ul>

    </div>*@


    <div class="company-head">
        <div class="left">
            <a href="@Url.Action("Main", "Home", new {area = "App", uid = Model.Uid})">&nbsp;<span><</span>&nbsp;返回</a>
        </div>
        <div class="middle">
            <p>案例(@Model.CaseCount)</p>
        </div>
        <div class="right">
            <p></p>
        </div>
    </div>
    <div class="lists-main">
        @foreach (var item in Model.PracticeCase)
        {
            <a href="@Url.Action("EditPracticeCase", "Home", new {area = "App", uid = Model.Uid, CaseNo = item.CaseNo, CaseName = item.CaseName})">
                <div class="listst">
                    <div class="list-left">
                        <p class="lists-a" href="">@item.CaseName</p>
                    </div>
                    <div class="list-right">
                        @Html.ActionLink("X", "DeletePracticeCase", "Home", new {uid = Model.Uid, CaseNo = item.CaseNo}, new {})
                    </div>
                    <p class="lists-p">创建于 @item.CaseTime.ToString("yyyy-M-d dddd")</p>
                </div>
            </a>
        }
    </div>

    <a href="@Url.Action("AddPracticeCase", "Home", new {area = "App", uid = Model.Uid})">
        <div class="do-it">
            <p class="do-it-a">添加案例</p>
        </div>
    </a>

</div>
</body>
