@model  List<object[]>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-primary">
            <div class="panel-heading">
                @ViewData["Title"]
            </div>
            <div class="panel-body">
                @Html.Action("_Carousel", "Home", new { model = Model })
            </div>
        </div>
    </div>
</div>
